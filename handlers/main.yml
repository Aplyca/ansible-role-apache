---
- name: restart apache
  sudo: yes
  service:
    name: "{{ apache['service'] }}"
    state: restarted
    enabled: yes

- name: reload apache
  sudo: yes
  service:
    name: "{{ apache['service'] }}"
    state: reloaded
    enabled: yes
