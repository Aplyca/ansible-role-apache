dependencies:
  pre:
    - sudo pip install -r build/requirements.txt

machine:
  hosts:
    myapp.com: 127.0.0.1

test:
  pre:
    - sudo ln -s ansible-role-aws ../aplyca.Apache
  override:
    - tests/tests.sh
