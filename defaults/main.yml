---
apache:
  Listen:
    - 80
  ServerTokens: "Prod"
  ServerSignature: "Off"
  enable_modules:
    - rewrite
  disable_modules:
    - autoindex
  extra_conf:
    z10: override
  virtualhosts:
    myapp.com:
      ServerName: myapp.com
