---
apache:
  Listen:
    - 80
  ServerTokens: "Prod"
  ServerSignature: "Off"
  enable_modules:
    - rewrite
    - expires
  disable_modules:
    - autoindex
  virtualhosts:
    - ServerName: myapp.com
