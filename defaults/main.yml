---
apache:
  Listen:
    - 80
  config:
    ServerName: "default"
  ssl:
    SSLHonorCipherOrder: "on"
  enable_modules:
    - rewrite
    - expires
    - ssl
  disable_modules:
    - autoindex
  virtualhosts: []
  certificates: []
  sections:   
    DirectoryMatch:
      - "/\\.git":
          - "Require all denied"
    FilesMatch:
      - "^\\.ht":
          - "Require all denied"
